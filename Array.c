#include <stdio.h>
int main)
int arr[10],i, sum=0;
printf ("enter 10 elements to
array");
for (i=0;i<10;i++)
{
scanf ("%d"
, &arr [1]):
sum=sum+arr [1];
}
printf ("sum of all array elements
i5%d", sum) ;
return 0;
}
